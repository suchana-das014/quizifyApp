<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Required permission for internet access (needed for Firebase or online features) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/Theme.Quizio"
        tools:targetApi="31">

        <!-- Result Activity -->
        <activity android:name=".ResultActivity" android:exported="true" />

        <!-- Add Question Activity -->
        <activity android:name=".AddQuestionActivity" android:exported="true" />

        <!-- Play Activity -->
        <activity android:name=".PlayActivity" android:exported="true" />

        <!-- Category Selection Activity -->
        <activity android:name=".CategoryActivity" android:exported="true" />

        <!-- Settings Activity -->
        <activity android:name=".SettingsActivity" android:exported="true" />

        <!-- Main Launcher Activity -->
        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
